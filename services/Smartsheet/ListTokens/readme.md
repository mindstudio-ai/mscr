# List Tokens

Lists all access tokens (API keys) for the authenticated user.

## Configuration
- **Output Variable**: Variable to store tokens

## Notes
- Shows all active tokens
- Includes creation dates
- Useful for token management
- Cannot see token values (only metadata)

